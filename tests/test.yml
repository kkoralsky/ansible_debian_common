---
- hosts: vagrant.local
  gather_facts: no
  roles:
    - role: debian_common
      bootstrap_as: vagrant
      bootstrap_use_sudo: yes
      setup_sudo: yes
      user: koral
      run_raw: true
      become: true
